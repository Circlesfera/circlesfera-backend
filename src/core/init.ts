/**
 * Este archivo debe ser importado ANTES que cualquier otro módulo que use decoradores de Typegoose.
 * Garantiza que reflect-metadata está cargado y activo.
 */
import 'reflect-metadata';

// Exportar un valor para evitar que el import sea eliminado como dead code
export const REFLECT_METADATA_LOADED = true;

